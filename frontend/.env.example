# Copia este archivo a ".env" (sin ".example") y rellena los valores reales.
# En produccion (Vercel), estas mismas variables se configuran en el panel de
# Vercel, no en un archivo.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_URL=
